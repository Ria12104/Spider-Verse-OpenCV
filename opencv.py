# opencv.py — backwards-compatibility shim
# The codebase has been split into focused modules. Run main.py instead.
# This file is kept so that any existing shortcuts/scripts still work.

from main import main

if __name__ == "__main__":
    main()
